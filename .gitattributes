# The default settings for all files.
# Use `text` if `.gitignore` allows tracking of only text files.
# Use `text=auto` if `.gitignore` allows tracking of both text and binary files.
* text=auto eol=lf whitespace=tab-in-indent,tabwidth=2

# Batch.
*.bat eol=crlf

# C.
*.c diff=cpp

# PowerShell.
*.cmd eol=crlf

# CSS.
*.css diff=css
*.pcss diff=css

# HTML.
*.html diff=html

# Markdown.
# 4 white spaces are used for indentation.
# 2 consecutive trailing white spaces indicate a line break.
*.md diff=markdown whitespace=-blank-at-eol,tab-in-indent,tabwidth=4
*.mdx diff=markdown whitespace=-blank-at-eol,tab-in-indent,tabwidth=4

# PHP.
*.php diff=php whitespace=tab-in-indent,tabwidth=4

# Python.
*.py diff=python whitespace=tab-in-indent,tabwidth=4

# Shell.
*.sh eol=lf diff=bash

# SQL.
# White spaces are used for indentation.
*.sql whitespace=tab-in-indent,tabwidth=2

# SVG.
# Should not be treated as binary data.
*.svg text

# Git.
.gitmodules whitespace=indent-with-non-tab,tabwidth=1
